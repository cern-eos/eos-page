CONTROLLER_SECRET=dev-secret
ADDR=:8080
DATA_DIR=data
# OPENAI_API_KEY=
# Responses API model your key can call (built-in web_search), e.g.
# gpt-4o-mini (default), gpt-4o, gpt-4.1, gpt-5-mini, gpt-5
# OPENAI_MODEL=gpt-4o-mini
# GEMINI_API_KEY=
# GOOGLE_API_KEY=
# CHAT_SEARCH_HEADED=1
# CHAT_SEARCH_CHROME=1
# CHAT_CHROME_PROFILE=data/chrome-profile
# CHAT_CHROME_BIN=/usr/bin/google-chrome
# GOOGLE_CSE_ID=
# GOOGLE_CSE_KEY=
# TLS_CERT=/path/to/fullchain.pem
# TLS_KEY=/path/to/privkey.pem
# PUBLIC_BASE_URL=https://eos.web.cern.ch
# EOS_GIT_DIR=/path/to/eos
# Local EOS clone for commit search and the Wall of Fame (git shortlog -sne --all)
